{% extends 'base.html.twig' %}

{% block title %}Hello ConferenceController!{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.createdAt) }}
    {{ form_row(form.add conference) }}
    {{ form_end(form) }}
{% endblock %}
